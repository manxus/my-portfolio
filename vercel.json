{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/((?!api(?:/|$))(?!admin(?:/|$))(?!assets(?:/|$)).*)",
      "destination": "/index.html"
    }
  ]
}